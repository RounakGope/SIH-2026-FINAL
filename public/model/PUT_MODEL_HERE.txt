Put the three files from Teachable Machine's "Export Model → TensorFlow.js → Download" here:

  model.json
  metadata.json
  weights.bin

Class names in Teachable Machine must be:
  healthy, bacterial_blight, leaf_curl, jassid_damage, other

Until these files exist the app uses a clearly-labelled DEMO predictor.
